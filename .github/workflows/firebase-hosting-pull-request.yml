# Builds pull requests and deploys a Firebase Hosting preview channel only.
# https://github.com/firebase/firebase-tools

name: Validate and preview Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    environment: Dev
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      checks: write
    steps:
      - uses: actions/checkout@v4
      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          token_format: access_token
          workload_identity_provider: "projects/904627453868/locations/global/workloadIdentityPools/github-pool/providers/github-provider"
          service_account: "github-firebase-deployer@showmaster1-f1a9f.iam.gserviceaccount.com"
      - name: Create .env for React build
        run: |
          cat > .env << 'EOF'
          REACT_APP_FIREBASE_API_KEY=${{ secrets.FIREBASE_API_KEY }}
          REACT_APP_FIREBASE_APP_ID=${{ secrets.FIREBASE_APP_ID }}
          REACT_APP_FIREBASE_AUTH_DOMAIN=${{ secrets.FIREBASE_AUTH_DOMAIN }}
          REACT_APP_FIREBASE_MEASUREMENT_ID=${{ secrets.FIREBASE_MEASUREMENT_ID }}
          REACT_APP_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          REACT_APP_FIREBASE_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }}
          REACT_APP_FIREBASE_STORAGE_BUCKET=${{ secrets.FIREBASE_STORAGE_BUCKET }}
          REACT_APP_STRIPE_PUBLISHABLE_KEY=${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          EOF
      - name: Install app dependencies
        run: npm ci
      - name: Install functions dependencies
        run: npm --prefix functions ci
      - name: Test app
        env:
          CI: true
          DISABLE_ESLINT_PLUGIN: true
        run: npm test -- --watchAll=false --passWithNoTests
      - name: Build app
        env:
          CI: false
          DISABLE_ESLINT_PLUGIN: true
        run: npm run build
      - name: Check functions syntax
        run: |
          git ls-files 'functions/**/*.js' functions/index.js | xargs -n 1 node --check
      - name: Deploy Firebase Hosting preview
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ env.GOOGLE_GHA_CREDS_PATH }}
        run: |
          npm install -g firebase-tools
          firebase hosting:channel:deploy "pr-${{ github.event.pull_request.number }}" --project showmaster1-f1a9f --expires 7d --non-interactive
